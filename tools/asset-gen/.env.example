# Asset generation credentials — copy to .env and fill in.
# .env is gitignored; never commit real keys (see AGENTS.md).

# Provider selection
IMAGE_PROVIDER=openai
# AUDIO_PROVIDER=   # configure a provider when audio generation is available
# VIDEO_PROVIDER=   # configure a provider when video generation is available

# Provider credentials
OPENAI_API_KEY=
